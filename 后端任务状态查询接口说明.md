# 后端任务状态查询接口说明

本文档用于说明后端调用 `GET /api/v1/task/{task_id}` 查询建树任务和字段提取任务时，应如何判断任务是否完成、进行中或失败。

## 1. 接口信息

- 方法：`GET`
- 路径：`/api/v1/task/{task_id}`
- 用途：根据 `task_id` 查询异步任务状态。

示例：

```bash
curl "http://10.67.75.27:8000/api/v1/task/a39e1289415e4c838dff9d7146300da0"
```

## 2. 统一响应结构

所有响应统一使用如下结构：

```json
{
  "code": 200,
  "message": "任务状态查询成功",
  "data": {}
}
```

字段说明：

- `code`：本次接口请求的业务状态码。
- `message`：本次接口请求的提示信息。
- `data`：任务状态数据。任务是否完成、失败或进行中，主要看这里面的 `status`。

重要说明：

`code = 200` 只表示“查询任务状态这个请求成功了”，不表示任务本身成功。

任务本身是否成功，必须判断：

```text
data.status
```

## 3. 任务状态判断依据

后端判断任务状态的核心字段是：

```json
{
  "data": {
    "status": "completed"
  }
}
```

`status` 目前有 4 种取值：

| status | 含义 | 后端处理建议 |
| --- | --- | --- |
| `pending` | 任务已创建，等待后台执行 | 继续轮询 |
| `processing` | 任务正在执行 | 继续轮询；如果有 `progress` 可展示进度 |
| `completed` | 任务执行成功 | 停止轮询，读取结果字段 |
| `failed` | 任务执行失败 | 停止轮询，读取 `data.error` |

推荐判断逻辑：

```python
task_data = response["data"]
status = task_data["status"]

if status == "completed":
    # 任务完成，读取 doc_id/tree_id 或 output_path
    pass
elif status == "failed":
    # 任务失败，读取失败原因
    error = task_data.get("error", "")
elif status in ("pending", "processing"):
    # 任务未结束，继续轮询
    pass
else:
    # 未知状态，建议按异常处理
    raise ValueError(f"Unknown task status: {status}")
```

## 4. 任务类型判断依据

任务类型字段是：

```json
{
  "data": {
    "task_type": "build_tree"
  }
}
```

`task_type` 目前有 2 种取值：

| task_type | 含义 |
| --- | --- |
| `build_tree` | 建树任务 |
| `extraction` | 字段提取任务 |

后端可以结合 `task_type` 和 `status` 判断下一步动作。

## 5. 建树任务返回示例

建树任务完成时，`data.status` 为 `completed`，并且会返回 `doc_id` 和 `tree_id`。

```json
{
  "code": 200,
  "message": "任务状态查询成功",
  "data": {
    "task_id": "a39e1289415e4c838dff9d7146300da0",
    "task_type": "build_tree",
    "status": "completed",
    "file_name": "甘露机岛合同-20181211版最终-签字版（无价格版）.docx",
    "file_path": "/home/sgc/PageIndex-MJ/artifacts/api_workspace/tasks/a39e1289415e4c838dff9d7146300da0/input/甘露机岛合同-20181211版最终-签字版（无价格版）.docx",
    "output_path": "",
    "error": "",
    "created_at": "2026-05-18T04:28:39Z",
    "workspace_dir": "/home/sgc/PageIndex-MJ/artifacts/api_workspace/workspace",
    "started_at": "2026-05-18T04:28:39Z",
    "output_dir": "/home/sgc/PageIndex-MJ/artifacts/api_workspace/tasks/a39e1289415e4c838dff9d7146300da0/output",
    "completed_at": "2026-05-18T04:38:48Z",
    "result_status": "success",
    "doc_id": "doc_1079388f5212c5d90f705bac4a6ad9612ff5d6cfa284802084d2ddb7d8544fab",
    "tree_id": "tree_7b9762e3f834d7c3416b544462b57e4a75dfffbafaafc9f350b3ef9c06f4ccb0",
    "source_file": "/home/sgc/PageIndex-MJ/artifacts/api_workspace/tasks/a39e1289415e4c838dff9d7146300da0/output/甘露机岛合同-20181211版最终-签字版（无价格版）.pdf"
  }
}
```

建树任务完成后，后端重点读取：

- `data.status`
- `data.task_type`
- `data.doc_id`
- `data.tree_id`
- `data.source_file`

推荐判断：

```python
if task_data["task_type"] == "build_tree" and task_data["status"] == "completed":
    doc_id = task_data["doc_id"]
    tree_id = task_data["tree_id"]
    # 使用 doc_id 发起后续字段提取任务
```

## 6. 字段提取任务返回示例

字段提取任务完成时，`data.status` 为 `completed`，并且会返回 `output_path`。

```json
{
  "code": 200,
  "message": "任务状态查询成功",
  "data": {
    "task_id": "bbf7051b87ff4de394c834526a74df8a",
    "task_type": "extraction",
    "doc_id": "doc_1079388f5212c5d90f705bac4a6ad9612ff5d6cfa284802084d2ddb7d8544fab",
    "status": "completed",
    "output_path": "/home/sgc/PageIndex-MJ/artifacts/api_workspace/tasks/bbf7051b87ff4de394c834526a74df8a/output/doc_1079388f5212c5d90f705bac4a6ad9612ff5d6cfa284802084d2ddb7d8544fab_extraction.json",
    "error": "",
    "created_at": "2026-05-18T04:43:41Z",
    "workspace_dir": "/home/sgc/PageIndex-MJ/artifacts/api_workspace/workspace",
    "schema_field_count": 2,
    "extracted_count": 2,
    "total_count": 2,
    "require_evidence": true,
    "started_at": "2026-05-18T04:43:41Z",
    "output_dir": "/home/sgc/PageIndex-MJ/artifacts/api_workspace/tasks/bbf7051b87ff4de394c834526a74df8a/output",
    "completed_at": "2026-05-18T04:44:11Z",
    "result_status": "success",
    "extraction_result": {
      "party_a": {
        "value": "潮州深能甘露热电有限公司",
        "page_number": [5],
        "section_title": "第一章 合同协议书 > 买方",
        "original_quote": "买方 名称：潮州深能甘露热电有限公司",
        "status": "found",
        "confidence": "High",
        "reason": "None"
      },
      "party_b": {
        "value": "上海电气集团股份有限公司",
        "page_number": [5],
        "section_title": "第一章 合同协议书 > 卖方",
        "original_quote": "卖方 名称：上海电气集团股份有限公司",
        "status": "found",
        "confidence": "High",
        "reason": "None"
      }
    }
  }
}
```

字段提取任务完成后，后端重点读取：

- `data.status`
- `data.task_type`
- `data.doc_id`
- `data.output_path`
- `data.extraction_result`
- `data.result_status`

推荐判断：

```python
if task_data["task_type"] == "extraction" and task_data["status"] == "completed":
    output_path = task_data["output_path"]
    extraction_result = task_data["extraction_result"]
    party_a = extraction_result["party_a"]
    party_b = extraction_result["party_b"]
    # 如需读取完整落盘文件，仍可使用 output_path
```

## 7. 处理中任务的进度字段

字段提取任务处于 `processing` 状态时，可能返回 `progress` 字段。

```json
{
  "code": 200,
  "message": "任务状态查询成功",
  "data": {
    "task_id": "example-task-id",
    "task_type": "extraction",
    "status": "processing",
    "extracted_count": 5,
    "total_count": 20,
    "progress": {
      "current": 5,
      "total": 20,
      "percent": 25.0
    }
  }
}
```

字段说明：

- `data.extracted_count`：已完成提取的字段数。
- `data.total_count`：总字段数。
- `data.progress.current`：当前已完成数量。
- `data.progress.total`：总数量。
- `data.progress.percent`：完成百分比。

注意：

建树任务通常没有字段级进度，字段提取任务才会返回字段级 `progress`。

## 8. 失败任务返回示例

当任务失败时，`data.status` 为 `failed`，失败原因在 `data.error`。

建树失败示例：

```json
{
  "code": 200,
  "message": "任务状态查询成功",
  "data": {
    "task_id": "fbf55cddb0bc40049a262f3279298b3a",
    "task_type": "build_tree",
    "status": "failed",
    "file_name": "甘露机岛合同-20181211版最终-签字版（无价格版）.pdf",
    "output_path": "",
    "error": "[Errno 2] No such file or directory: 'java'",
    "created_at": "2026-05-18T02:39:14Z",
    "started_at": "2026-05-18T02:39:14Z",
    "completed_at": "2026-05-18T02:39:14Z"
  }
}
```

字段提取失败示例：

```json
{
  "code": 200,
  "message": "任务状态查询成功",
  "data": {
    "task_id": "bbf7051b87ff4de394c834526a74df8a",
    "task_type": "extraction",
    "doc_id": "doc_1079388f5212c5d90f705bac4a6ad9612ff5d6cfa284802084d2ddb7d8544fab",
    "status": "failed",
    "output_path": "",
    "error": "字段提取失败的具体原因",
    "created_at": "2026-05-18T04:43:41Z",
    "workspace_dir": "/home/sgc/PageIndex-MJ/artifacts/api_workspace/workspace",
    "schema_field_count": 2,
    "extracted_count": 0,
    "total_count": 2,
    "require_evidence": true,
    "started_at": "2026-05-18T04:43:41Z",
    "output_dir": "/home/sgc/PageIndex-MJ/artifacts/api_workspace/tasks/bbf7051b87ff4de394c834526a74df8a/output",
    "completed_at": "2026-05-18T04:43:45Z"
  }
}
```

推荐判断：

```python
if task_data["status"] == "failed":
    error = task_data.get("error", "")
    # 记录错误并返回给调用方
```

如果需要区分字段提取失败，可以同时判断 `task_type`：

```python
if task_data["task_type"] == "extraction" and task_data["status"] == "failed":
    error = task_data.get("error", "")
    # 字段提取任务失败，停止轮询并处理错误
```

## 9. 任务不存在

如果 `task_id` 不存在，接口会返回 HTTP `404`。

```json
{
  "code": 404,
  "message": "任务不存在",
  "data": null
}
```

这种情况说明传入的 `task_id` 无效、服务重启导致内存任务记录丢失，或查询了错误环境中的任务。

## 10. 推荐轮询流程

推荐后端按固定间隔轮询，例如每 2 到 5 秒查询一次。

```python
import time


def wait_task_done(task_id: str, query_task, interval_seconds: int = 2, timeout_seconds: int = 1800):
    start_time = time.time()

    while True:
        response = query_task(task_id)
        task_data = response["data"]
        status = task_data["status"]

        if status == "completed":
            return task_data

        if status == "failed":
            raise RuntimeError(task_data.get("error", "任务执行失败"))

        if status not in ("pending", "processing"):
            raise RuntimeError(f"未知任务状态: {status}")

        if time.time() - start_time > timeout_seconds:
            raise TimeoutError(f"任务超时: {task_id}")

        time.sleep(interval_seconds)
```

## 11. 后端对接要点

- 不要用外层 `code == 200` 判断任务完成。
- 判断任务完成、进行中、失败时，始终使用 `data.status`。
- 判断任务类型时，使用 `data.task_type`。
- 建树任务完成后，读取 `data.doc_id`，用于后续字段提取。
- 字段提取任务完成后，优先读取 `data.extraction_result`，该对象会沿用动态 schema 的字段名，例如 `party_a`、`party_b`。
- 字段提取任务完成后，`data.output_path` 仍会保留，用于需要读取落盘 JSON 文件的场景。
- 字段提取任务失败时，`data.task_type == "extraction"` 且 `data.status == "failed"`，失败原因在 `data.error`。
- 任务失败时，读取 `data.error`。
- `pending` 和 `processing` 都表示任务未结束，需要继续轮询。
